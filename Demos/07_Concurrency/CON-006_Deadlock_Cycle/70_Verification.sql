SET NOCOUNT ON;SET XACT_ABORT ON;IF (SELECT COUNT(*) FROM lab.Evidence WHERE Outcome='COMPLETED')<>2 OR EXISTS(SELECT 1 FROM lab.Evidence WHERE Outcome='VICTIM') THROW 51006,'FAIL_RESULT_CONTRACT: Geordnete Gegenprobe fehlgeschlagen.',1;SELECT * FROM lab.Evidence;PRINT 'SQLPERF_SUMMARY|PASS|OK';
